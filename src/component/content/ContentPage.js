import React, { PropTypes } from 'react';

const Content = () => {
    return (
        <div className='content-component'>
            <h1>No contentent</h1>
        </div>
    );
};

export default Content;
